spawn different sub agents to get the job done. 

you can run these in parallel or sequentially. parallel if they do not overlap each other. sequential if they do.

ensure you provided these subagents with enough context to get the job done.